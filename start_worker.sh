#!/bin/bash
# 맥 자동 포스터 워커 실행 (폰에서 '맥 자동저장' 요청한 글을 네이버에 자동 임시저장).
# caffeinate -i : 워커가 도는 동안 맥이 idle 로 잠들지 않게 막음(화면은 꺼져도 됨).
#   ※ 노트북 뚜껑을 닫으면 강제 sleep 되어 멈춥니다(깨어나면 자동 재개).
cd "$(dirname "$0")"
echo "================================================================"
echo "  맥 자동 포스터 워커 (Ctrl+C 로 종료)"
echo "  폰에서 글 저장 시 '맥에서 자동 임시저장'을 체크하면 여기서 처리합니다."
echo "  처음 1회는 mac_poster.py 로 네이버 로그인을 해두세요."
echo "================================================================"
caffeinate -i python3 mac_worker.py
